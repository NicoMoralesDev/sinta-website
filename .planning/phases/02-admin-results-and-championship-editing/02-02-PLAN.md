---
phase: 02-admin-results-and-championship-editing
plan: 02
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - app/admin/_components/events-manager.tsx
  - app/admin/_components/championships-manager.tsx
  - app/admin/events/page.tsx
  - app/admin/championships/page.tsx
  - tests/admin-events-manager.flow.spec.ts
  - tests/admin-championships-manager.flow.spec.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
must_haves:
  truths:
    - "Admins can open the existing event results workflow and edit `QS`, `S`, `QF`, `F`, and `P` in one responsive grid."
    - "The editor gives light helper text, highlights invalid cells inline, blocks save on invalid input, and serializes only dirty cells using the Phase 2 clear/value contract."
    - "Championship create/update forms expose organizer metadata as optional secondary information without disrupting the current admin layout."
  artifacts:
    - path: "app/admin/_components/events-manager.tsx"
      provides: "Responsive five-column results editor with dirty tracking and points-safe validation UX"
    - path: "app/admin/_components/championships-manager.tsx"
      provides: "Organizer field exposure in create and edit flows"
    - path: "tests/admin-events-manager.flow.spec.ts"
      provides: "Static render and helper coverage for canonical editor labels, helper text, and serializer behavior"
    - path: "tests/admin-championships-manager.flow.spec.ts"
      provides: "Static render coverage for organizer field presence and optional treatment"
  key_links:
    - from: "app/admin/_components/events-manager.tsx"
      to: "app/api/v1/admin/events/[id]/results/route.ts"
      via: "dirty serializer sends only changed canonical cells and explicit clear tombstones"
      pattern: "fetch(`/api/v1/admin/events/${selectedEventId}/results`"
    - from: "app/admin/_components/events-manager.tsx"
      to: "lib/server/admin/types.ts"
      via: "fieldOrder and fieldLabels drive desktop/mobile header rendering"
      pattern: "fieldOrder|fieldLabels"
    - from: "app/admin/_components/championships-manager.tsx"
      to: "app/api/v1/admin/championships/route.ts"
      via: "POST and PATCH bodies include organizerName"
      pattern: "organizerName"
---

<objective>
Expose the canonical results and organizer metadata contracts in the existing admin UI without redesigning the admin area.

Purpose: Turn the widened backend contract into a usable admin workflow that matches the user’s layout, validation, and secondary-metadata preferences.
Output: Responsive results editor UI, organizer field UI, and lightweight render/helper tests.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-results-and-championship-editing/02-CONTEXT.md
@.planning/phases/02-admin-results-and-championship-editing/02-RESEARCH.md
@.planning/phases/02-admin-results-and-championship-editing/02-VALIDATION.md
@.planning/phases/02-admin-results-and-championship-editing/02-01-PLAN.md
@.skills/version-aware-implementation/SKILL.md
@app/admin/_components/events-manager.tsx
@app/admin/_components/championships-manager.tsx
@app/admin/events/page.tsx
@app/admin/championships/page.tsx

<interfaces>
From `lib/server/admin/types.ts`:
```ts
export type AdminEventResultsGrid = {
  event: AdminEvent;
  fieldOrder: ["qs", "s", "qf", "f", "p"];
  fieldLabels: Record<AdminCanonicalResultField, string>;
  drivers: Array<{
    driverId: string;
    driverSlug: string;
    driverName: string;
    results: Partial<Record<AdminCanonicalResultField, {
      position: number | null;
      status: ResultStatus | null;
      rawValue: string;
      isActive: boolean;
    }>>;
  }>;
};
```

From `app/admin/_components/events-manager.tsx`:
```ts
async function loadGrid(eventId: string): Promise<void>
async function saveResults(): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the responsive canonical results editor</name>
  <read_first>
    app/admin/_components/events-manager.tsx
    app/admin/events/page.tsx
    lib/server/admin/types.ts
    .planning/phases/02-admin-results-and-championship-editing/02-CONTEXT.md
    .planning/phases/02-admin-results-and-championship-editing/02-RESEARCH.md
  </read_first>
  <files>app/admin/_components/events-manager.tsx, app/admin/events/page.tsx, tests/admin-events-manager.flow.spec.ts</files>
  <action>Replace the current two-column editor state in `app/admin/_components/events-manager.tsx` with a canonical five-field model keyed by `qs`, `s`, `qf`, `f`, and `p`. Render the existing event row expander as a responsive grid/table that always shows all five canonical fields. On desktop use friendly labels from `grid.fieldLabels`; on mobile or tight layouts fall back to compact `QS`, `S`, `QF`, `F`, `P` headers. Add light helper text that says race fields accept positive integers or `DNF / DNQ / DSQ / ABSENT`, while points accepts whole numbers `>= 0`. Track dirty cells by `(driverId, field)` so save serializes only changed cells. Use the Phase 2 backend contract: changed populated cells send `isActive: true`; cleared cells send `isActive: false`, `position: null`, `status: null`, `rawValue: ""`. Block save when any cell is invalid, mark invalid inputs with inline styling plus `aria-invalid`, and focus the first invalid cell before returning. Extract small pure helpers from the component as needed so `tests/admin-events-manager.flow.spec.ts` can assert label selection, parser behavior, and dirty-row serialization without a browser harness. Keep the page title/subtitle aligned with canonical editing if `app/admin/events/page.tsx` needs copy updates.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/admin-events-manager.flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/_components/events-manager.tsx` renders all five canonical field keys `qs`, `s`, `qf`, `f`, and `p`.
    - `app/admin/_components/events-manager.tsx` contains helper copy for race fields and points fields.
    - `app/admin/_components/events-manager.tsx` uses compact `QS`, `S`, `QF`, `F`, `P` labels in a tight-layout branch or helper.
    - `app/admin/_components/events-manager.tsx` marks invalid cells with `aria-invalid`.
    - `tests/admin-events-manager.flow.spec.ts` asserts dirty-row serialization produces explicit clear tombstones for emptied cells.
  </acceptance_criteria>
  <done>Admins can edit canonical results in the existing workflow with validation feedback that matches the backend contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose organizer metadata in championship management</name>
  <read_first>
    app/admin/_components/championships-manager.tsx
    app/admin/championships/page.tsx
    tests/championship-organizer.spec.ts
    .planning/phases/02-admin-results-and-championship-editing/02-CONTEXT.md
  </read_first>
  <files>app/admin/_components/championships-manager.tsx, app/admin/championships/page.tsx, tests/admin-championships-manager.flow.spec.ts</files>
  <action>Extend the championships manager drafts and forms to include `organizerName` as optional secondary metadata. Add an inline `Organizador` input to both the create form and the editable table rows without pushing it above the primary season/name/session fields. Keep the field optional, with blank values remaining visually simple and no extra format rules beyond the label itself. Make both POST and PATCH request bodies include `organizerName`, reusing existing success/error handling and `router.refresh()` behavior. If helpful for testability, extract a small payload builder or normalizer so `tests/admin-championships-manager.flow.spec.ts` can verify organizer data is present in create/edit payload shaping and that the field renders in the current UI structure. Update `app/admin/championships/page.tsx` subtitle only if needed to mention organizer metadata explicitly, but keep the page visually aligned with the current admin shell.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/admin-championships-manager.flow.spec.ts tests/championship-organizer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/_components/championships-manager.tsx` contains an `organizerName` draft field.
    - `app/admin/_components/championships-manager.tsx` renders an input labeled `Organizador` in both create and edit flows.
    - `app/admin/_components/championships-manager.tsx` includes `organizerName` in the POST request body.
    - `app/admin/_components/championships-manager.tsx` includes `organizerName` in the PATCH request body.
    - `tests/admin-championships-manager.flow.spec.ts` covers organizer field rendering or payload shaping without introducing a browser-only test dependency.
  </acceptance_criteria>
  <done>Admins can create and update championship organizer metadata from the existing championships screen.</done>
</task>

</tasks>

<verification>
Run the new admin UI-focused specs plus typecheck. Confirm the editor exposes five canonical fields, invalid values are blocked before save, and championship forms surface organizer metadata as optional secondary information.
</verification>

<success_criteria>
The admin UI now matches the canonical results model and organizer metadata contract while preserving the existing workflow and visual system.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-results-and-championship-editing/02-02-SUMMARY.md`
</output>
