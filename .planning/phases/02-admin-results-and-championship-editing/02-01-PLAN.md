---
phase: 02-admin-results-and-championship-editing
plan: 01
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/server/admin/types.ts
  - lib/server/admin/repository.ts
  - lib/server/admin/service.ts
  - app/api/v1/admin/events/[id]/results/route.ts
  - tests/admin-event-results-contract.spec.ts
  - tests/admin-event-results-preserve.spec.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "The admin event-results GET contract exposes canonical field order `qs`, `s`, `qf`, `f`, `p` plus friendly labels so the existing event workflow can render a five-column editor without guessing."
    - "Updating or clearing one result cell does not drop unrelated persisted rows for the same event."
    - "Points rows accept only integer numeric values `>= 0`, while race-session rows continue to accept positive integer finishes or valid status tokens."
  artifacts:
    - path: "lib/server/admin/types.ts"
      provides: "Canonical admin results-grid and write-patch contracts"
    - path: "lib/server/admin/repository.ts"
      provides: "Canonical grid loading and clear-aware row replacement"
    - path: "lib/server/admin/service.ts"
      provides: "Points-safe validation and merge/delete semantics for dirty cell patches"
    - path: "tests/admin-event-results-contract.spec.ts"
      provides: "GET/PUT contract coverage for canonical grid metadata and clear tombstones"
    - path: "tests/admin-event-results-preserve.spec.ts"
      provides: "Regression coverage for preserve-on-omit plus explicit clear behavior"
  key_links:
    - from: "lib/server/admin/types.ts"
      to: "app/admin/_components/events-manager.tsx"
      via: "fieldOrder, fieldLabels, and per-driver canonical result map shape"
      pattern: "fieldOrder|fieldLabels|results"
    - from: "app/api/v1/admin/events/[id]/results/route.ts"
      to: "lib/server/admin/service.ts"
      via: "PUT accepts dirty changed cells, including explicit clear tombstones"
      pattern: "isActive"
    - from: "lib/server/admin/service.ts"
      to: "lib/server/admin/repository.ts"
      via: "merge incoming patches with persisted rows and delete rows when clear tombstones are present"
      pattern: "mergeEventResultCells|replaceEventResults"
---

<objective>
Widen the admin results backend contract so the existing event workflow can load and save canonical `QS/S/QF/F/P` cells safely.

Purpose: Convert the Phase 1 backend compatibility layer into a real Phase 2 admin editing contract before changing the UI.
Output: Canonical admin grid metadata, clear-aware dirty cell writes, and regression coverage for GET/PUT semantics.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-results-and-championship-editing/02-CONTEXT.md
@.planning/phases/02-admin-results-and-championship-editing/02-RESEARCH.md
@.planning/phases/02-admin-results-and-championship-editing/02-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-02-SUMMARY.md
@.skills/version-aware-implementation/SKILL.md
@lib/server/admin/types.ts
@lib/server/admin/repository.ts
@lib/server/admin/service.ts
@app/api/v1/admin/events/[id]/results/route.ts
@tests/admin-event-results-contract.spec.ts
@tests/admin-event-results-preserve.spec.ts

<interfaces>
From `lib/server/admin/service.ts`:
```ts
export async function getEventResults(actor: AdminActor, eventId: string): Promise<AdminEventResultsGrid>
export async function updateEventResults(
  actor: AdminActor,
  eventId: string,
  input: { rows: EventResultCellInput[] },
  options?: RequestOptions,
): Promise<AdminWriteResult<{ rows: EventResultCellInput[] }>>
```

From `lib/server/admin/repository.ts`:
```ts
export async function getEventResultsGrid(eventId: string): Promise<AdminEventResultsGrid | null>
export async function replaceEventResults(
  eventId: string,
  rows: EventResultCellInput[],
): Promise<AdminEventResultRow[]>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish a canonical admin results-grid contract</name>
  <read_first>
    lib/server/admin/types.ts
    lib/server/admin/repository.ts
    app/api/v1/admin/events/[id]/results/route.ts
    tests/admin-event-results-contract.spec.ts
    .planning/phases/02-admin-results-and-championship-editing/02-RESEARCH.md
  </read_first>
  <files>lib/server/admin/types.ts, lib/server/admin/repository.ts, app/api/v1/admin/events/[id]/results/route.ts, tests/admin-event-results-contract.spec.ts</files>
  <action>Replace the two-slot `primary` / `secondary` admin grid contract with a canonical metadata-driven shape. Update `AdminEventResultsGrid` in `lib/server/admin/types.ts` so it contains `fieldOrder: ["qs", "s", "qf", "f", "p"]`, `fieldLabels` keyed by each canonical field, and per-driver `results` keyed by canonical field instead of `primary` / `secondary`. In `lib/server/admin/repository.ts`, make `getEventResultsGrid(eventId)` map stored rows into canonical keys, preserve absent historical fields by omitting them from the per-driver map, and build friendly labels as `qs -> "Qualy Sprint"`, `s -> championship.primarySessionLabel`, `qf -> "Qualy Final"`, `f -> championship.secondarySessionLabel`, `p -> "Puntos"`. Keep driver ordering by `sort_name`. Extend `tests/admin-event-results-contract.spec.ts` so the admin results GET/PUT seam asserts the widened grid shape and canonical field metadata instead of only legacy write normalization.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/admin-event-results-contract.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/server/admin/types.ts` contains `fieldOrder` and `fieldLabels` on `AdminEventResultsGrid`.
    - `lib/server/admin/types.ts` no longer models the grid as only `primary` and `secondary` result slots.
    - `lib/server/admin/repository.ts` emits canonical result keys `qs`, `s`, `qf`, `f`, and `p`.
    - `tests/admin-event-results-contract.spec.ts` asserts GET contract metadata for canonical field order and friendly labels.
    - `app/api/v1/admin/events/[id]/results/route.ts` continues to return `{ ok: true, grid }` with the widened canonical grid payload.
  </acceptance_criteria>
  <done>The admin results loader exposes a typed canonical five-field grid that the client can render directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Support dirty cell patches and explicit clears safely</name>
  <read_first>
    lib/server/admin/service.ts
    lib/server/admin/repository.ts
    app/api/v1/admin/events/[id]/results/route.ts
    lib/server/admin/types.ts
    tests/admin-event-results-contract.spec.ts
    tests/admin-event-results-preserve.spec.ts
    .planning/phases/02-admin-results-and-championship-editing/02-CONTEXT.md
  </read_first>
  <files>lib/server/admin/service.ts, lib/server/admin/repository.ts, app/api/v1/admin/events/[id]/results/route.ts, tests/admin-event-results-contract.spec.ts, tests/admin-event-results-preserve.spec.ts</files>
  <action>Keep the existing PUT route but change its write semantics from implicit snapshot preservation to explicit dirty cell patches. Continue to accept canonical `sessionKind` values only for Phase 2 UI writes. A changed populated cell must use `isActive: true` plus either `position` or `status`. An intentional clear must be represented as `isActive: false`, `position: null`, `status: null`, and `rawValue: ""`. Update `validateResultCell()` so this clear tombstone shape is valid, keep race-session rules as positive integer or one of `DNF | DNQ | DSQ | ABSENT`, and enforce points-field rules as integer `>= 0` or blank only, with no status tokens allowed for `p`. Update `mergeEventResultCells()` so omitted persisted rows are still preserved, but matching rows are removed when the incoming patch is an inactive clear tombstone. Extend `tests/admin-event-results-contract.spec.ts` and `tests/admin-event-results-preserve.spec.ts` so they cover single-cell updates, explicit clears, preserved untouched rows, rejected points statuses, and rejected non-integer or negative points values.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/admin-event-results-contract.spec.ts tests/admin-event-results-preserve.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/server/admin/service.ts` accepts clear tombstones where `isActive` is `false` and every value field is empty.
    - `lib/server/admin/service.ts` rejects `sessionKind: "p"` rows that contain `status`.
    - `lib/server/admin/service.ts` rejects non-integer or negative numeric values for `sessionKind: "p"`.
    - `lib/server/admin/repository.ts` no longer preserves a row when an incoming patch for the same `(driverId, sessionKind)` is an inactive clear tombstone.
    - `tests/admin-event-results-preserve.spec.ts` proves untouched rows remain after editing one cell and proves a cleared canonical row is actually removed.
  </acceptance_criteria>
  <done>The admin results write path can update or clear one canonical cell safely without harming unrelated persisted data.</done>
</task>

</tasks>

<verification>
Run the admin results contract and preserve specs. Confirm the widened GET payload exposes canonical metadata, and confirm PUT can distinguish changed, unchanged, and cleared cells.
</verification>

<success_criteria>
The backend/admin contract is ready for a five-column UI, and cell-level edits are safe enough that the frontend can serialize only the user’s intentional changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-results-and-championship-editing/02-01-SUMMARY.md`
</output>
