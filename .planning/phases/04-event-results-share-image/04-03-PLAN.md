---
phase: 04-event-results-share-image
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - app/api/v1/results/events/[id]/image/route.ts
  - app/results/page.tsx
  - lib/server/history/repository.ts
  - lib/server/history/service.ts
  - tests/history-share-image-route.spec.ts
  - tests/results-page.flow.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SHARE-01
  - SHARE-02
gap_closure: true
must_haves:
  truths:
    - "When `/results` is filtered by driver, each event share link preserves that driver filter in the image-route contract."
    - "A driver-filtered share image renders the same driver set as the selected public event table instead of silently expanding back to the full event."
    - "Requirement tracking marks Phase 4 sharing complete only after driver-filter parity is implemented and covered."
  artifacts:
    - path: "app/results/page.tsx"
      provides: "Driver-aware share-image href builder that preserves the active `/results` driver slug"
    - path: "lib/server/history/service.ts"
      provides: "Validated by-id event participation read that accepts an optional driver slug"
    - path: "lib/server/history/repository.ts"
      provides: "By-id event participation query that reuses the existing filtered results fetch path"
    - path: "app/api/v1/results/events/[id]/image/route.ts"
      provides: "Share-image route that forwards the optional driver slug without changing cache or language behavior"
    - path: "tests/history-share-image-route.spec.ts"
      provides: "Automated parity coverage for filtered-driver image output and mismatched-driver not-found handling"
    - path: "tests/results-page.flow.spec.ts"
      provides: "Flow coverage for filtered `/results` share links preserving event id, driver slug, and language"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Correct SHARE-01 and SHARE-02 traceability after the gap is closed"
  key_links:
    - from: "app/results/page.tsx"
      to: "app/api/v1/results/events/[id]/image/route.ts"
      via: "share href builder that carries `eventId`, optional `driver`, and current language"
      pattern: "buildResultsShareImageHref"
    - from: "app/api/v1/results/events/[id]/image/route.ts"
      to: "lib/server/history/service.ts"
      via: "by-id share-image read with optional driver slug"
      pattern: "getResultsEventParticipationById"
    - from: "lib/server/history/service.ts"
      to: "lib/server/history/repository.ts"
      via: "validated repository lookup with optional driver filtering"
      pattern: "getEventParticipationCardById"
    - from: "tests/results-page.flow.spec.ts"
      to: "app/results/page.tsx"
      via: "static-markup assertions for filtered share href parity"
      pattern: "driver=kevin-fontana"
    - from: "tests/history-share-image-route.spec.ts"
      to: "app/api/v1/results/events/[id]/image/route.ts"
      via: "route assertions that the generated image respects the selected driver set"
      pattern: "getResultsEventParticipationByIdMock"
---

<objective>
Close the Phase 4 verification gap by keeping driver-filtered `/results` tables and generated share images on the same participant set.

Purpose: Preserve the existing share feature in filtered results with the smallest safe change. Instead of suppressing sharing under a driver filter or adding a second DTO, widen the existing image-route contract with one optional validated `driver` slug and reuse the repository's existing filtered results path.
Output: Driver-aware share-image contract, automated driver-set parity coverage, and corrected Phase 4 requirement traceability.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-event-results-share-image/04-VERIFICATION.md
@.planning/phases/04-event-results-share-image/04-RESEARCH.md
@.planning/phases/04-event-results-share-image/04-VALIDATION.md
@.planning/phases/04-event-results-share-image/04-01-SUMMARY.md
@.planning/phases/04-event-results-share-image/04-02-SUMMARY.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-01-SUMMARY.md
@.skills/planning-and-verification/SKILL.md
@.skills/version-aware-implementation/SKILL.md
@.skills/db-change-safety/SKILL.md
@app/results/page.tsx
@app/api/v1/results/events/[id]/image/route.ts
@lib/server/history/service.ts
@lib/server/history/repository.ts
@tests/results-page.flow.spec.ts
@tests/history-share-image-route.spec.ts

<interfaces>
From `app/results/page.tsx`:
```ts
function buildResultsShareImageHref(eventId: string, lang: "es" | "en"): string;
```

From `lib/server/history/service.ts`:
```ts
export async function getResultsEventParticipationById(
  eventId: string,
): Promise<EventParticipationCard>;
```

From `lib/server/history/repository.ts`:
```ts
async function fetchResultsForEvents(
  eventIds: string[],
  driverSlug?: string,
): Promise<DbEventResultRow[]>;

export async function getEventParticipationCardById(
  eventId: string,
): Promise<EventParticipationCard | null>;
```

From `lib/server/history/types.ts`:
```ts
export type EventQuery = {
  year?: number;
  championship?: string;
  championshipId?: string;
  driver?: string;
  limit: number;
  offset: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the by-id share-image contract to preserve the optional driver filter</name>
  <read_first>
    app/api/v1/results/events/[id]/image/route.ts
    lib/server/history/service.ts
    lib/server/history/repository.ts
    tests/history-share-image-route.spec.ts
    .planning/phases/04-event-results-share-image/04-VERIFICATION.md
    .planning/phases/04-event-results-share-image/04-RESEARCH.md
  </read_first>
  <files>app/api/v1/results/events/[id]/image/route.ts, lib/server/history/service.ts, lib/server/history/repository.ts, tests/history-share-image-route.spec.ts</files>
  <behavior>
    - A share-image request may include one optional `driver` slug, and the generated image uses only that driver subset when present.
    - Invalid `driver` query values fail with the existing slug validation behavior instead of being ignored.
    - If an event exists but the requested `driver` is not present in that event's public results, the route returns `404` rather than generating an empty or full-event image.
    - Existing unfiltered requests keep the same cache headers, canonical columns, points-based ordering, and language behavior.
  </behavior>
  <action>Preserve the share feature under driver filtering by widening the existing route contract instead of suppressing the link. In `lib/server/history/repository.ts`, change `getEventParticipationCardById` to accept `driverSlug?: string`, pass that slug into `fetchResultsForEvents([eventId], driverSlug)`, and return `null` when a filtered by-id lookup yields zero participant rows so mismatched manual URLs become not-found instead of blank images. In `lib/server/history/service.ts`, change `getResultsEventParticipationById` to accept `driverSlug?: string`, validate it with the existing `parseSlug(..., "driver")`, and delegate the normalized slug to the repository. In `app/api/v1/results/events/[id]/image/route.ts`, read `driver` from `url.searchParams`, keep `lang` resolution and cache headers unchanged, and call `getResultsEventParticipationById(id, driver ?? undefined)`. Extend `tests/history-share-image-route.spec.ts` to cover: exact driver forwarding into the service call, filtered-driver markup containing only the selected driver, `404` when `driver` does not belong to the event, and unchanged success behavior for unfiltered requests. Do not add a new JSON route, do not create a second DTO, and do not change the existing `ImageResponse` layout or cache policy.</action>
  <verify>
    <automated>npx vitest run tests/history-share-image-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/server/history/repository.ts` contains `getEventParticipationCardById(` with an optional `driverSlug?: string` parameter.
    - `lib/server/history/repository.ts` contains `fetchResultsForEvents([eventId], driverSlug)`.
    - `lib/server/history/service.ts` contains `getResultsEventParticipationById(` with an optional `driverSlug?: string` parameter.
    - `lib/server/history/service.ts` contains `parseSlug(driverSlug ?? null, "driver")`.
    - `app/api/v1/results/events/[id]/image/route.ts` contains `const driver = url.searchParams.get("driver")`.
    - `app/api/v1/results/events/[id]/image/route.ts` contains `getResultsEventParticipationById(id, driver ?? undefined)`.
    - `tests/history-share-image-route.spec.ts` contains a request URL with `?driver=kevin-fontana`.
    - `tests/history-share-image-route.spec.ts` contains a `404` assertion for a mismatched filtered-driver request.
  </acceptance_criteria>
  <done>The by-id image route can faithfully render the same filtered driver subset that `/results` shows, while invalid or mismatched filters fail safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the active driver filter into `/results` share links and lock page-to-route parity</name>
  <read_first>
    app/results/page.tsx
    tests/results-page.flow.spec.ts
    app/components/event-participation-list.tsx
    .planning/phases/04-event-results-share-image/04-VERIFICATION.md
    .planning/phases/04-event-results-share-image/04-02-SUMMARY.md
  </read_first>
  <files>app/results/page.tsx, tests/results-page.flow.spec.ts</files>
  <behavior>
    - Unfiltered event cards keep the current deterministic share hrefs.
    - When `/results` has an active `driver` filter, each event card share link includes that exact slug so the image route can render the same driver set.
    - The page continues to use the canonical `eventId` route segment; championship filters, pagination, and language links stay intact.
  </behavior>
  <action>In `app/results/page.tsx`, widen `buildResultsShareImageHref` to accept the optional active `driver` slug from the page search params and append it to the image URL only when present. Preserve the current Spanish default and English `?lang=en` behavior; for filtered English links the query string should be `driver=<slug>&lang=en`, and for filtered Spanish links it should be `driver=<slug>` only. Keep the action rendered through the existing `renderEventActions` prop and continue building links from `event.eventId`, not championship slug, round number, or any client-side table snapshot. Update `tests/results-page.flow.spec.ts` so the filtered-driver scenario asserts exact href parity such as `/api/v1/results/events/event-1/image?driver=kevin-fontana&amp;lang=en`, while the existing unfiltered link, pagination, and language-switch assertions remain in place.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts tests/history-share-image-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/results/page.tsx` contains `buildResultsShareImageHref(` with an optional `driver?: string` parameter.
    - `app/results/page.tsx` contains `params.set("driver", driver)`.
    - `app/results/page.tsx` passes the page-level `driver` value into `buildResultsShareImageHref`.
    - `tests/results-page.flow.spec.ts` contains `/api/v1/results/events/event-1/image?driver=kevin-fontana&amp;lang=en`.
    - `tests/results-page.flow.spec.ts` still contains `/api/v1/results/events/event-1/image?lang=en` for the unfiltered path.
    - `tests/results-page.flow.spec.ts` still contains an assertion with `cursor=cursor-2`.
  </acceptance_criteria>
  <done>The public results page builds share-image links that preserve the visible driver-filter state without changing the existing event-id-based routing model.</done>
</task>

<task type="auto">
  <name>Task 3: Correct Phase 4 requirement traceability after the parity gap is closed</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/04-event-results-share-image/04-VERIFICATION.md
    .planning/phases/04-event-results-share-image/04-03-PLAN.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>Update `.planning/REQUIREMENTS.md` only after Tasks 1 and 2 are green so the requirement state matches the implementation. Mark `SHARE-01` complete in the checklist and traceability table, keep `SHARE-02` complete, and leave the rest of the milestone status untouched. Do not widen scope to Phase 5 docs or rewrite other requirement text.</action>
  <verify>
    <automated>rg -n "SHARE-01|SHARE-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **SHARE-01**`.
    - `.planning/REQUIREMENTS.md` contains `- [x] **SHARE-02**`.
    - `.planning/REQUIREMENTS.md` contains `| SHARE-01 | Phase 4 | Complete |`.
    - `.planning/REQUIREMENTS.md` contains `| SHARE-02 | Phase 4 | Complete |`.
  </acceptance_criteria>
  <done>Requirement status and traceability no longer contradict the verified implementation for Phase 4 sharing.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/history-share-image-route.spec.ts tests/results-page.flow.spec.ts`, then run `npm run typecheck` before closing the plan. The final evidence must show filtered-driver share URLs and filtered-driver image output staying on the same driver set.
</verification>

<success_criteria>
Phase 4 no longer has a filtered-driver parity gap: `/results` share links preserve the active driver slug, the image route renders the same selected driver set or returns `404` for mismatched manual filters, and `REQUIREMENTS.md` reflects both sharing requirements as complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-results-share-image/04-03-SUMMARY.md`
</output>
