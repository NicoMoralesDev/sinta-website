---
phase: 04-event-results-share-image
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/results/page.tsx
  - tests/results-page.flow.spec.ts
autonomous: true
requirements:
  - SHARE-01
  - SHARE-02
must_haves:
  truths:
    - "Visitors can trigger a shareable image for each public event card directly from `/results`."
    - "Each trigger points to one deterministic image route for that event id instead of encoding client-side table state."
    - "The trigger wiring keeps the existing `/results` filter and pagination behavior intact."
  artifacts:
    - path: "app/results/page.tsx"
      provides: "Minimal bilingual share-image trigger wiring for each public event card"
    - path: "tests/results-page.flow.spec.ts"
      provides: "Flow coverage for share-link copy, deterministic href generation, and coexistence with the existing results-page links"
  key_links:
    - from: "app/results/page.tsx"
      to: "app/components/event-participation-list.tsx"
      via: "the `renderEventActions` prop created in 04-01"
      pattern: "renderEventActions"
    - from: "app/results/page.tsx"
      to: "app/api/v1/results/events/[id]/image/route.ts"
      via: "share href builder using the event id and current language"
      pattern: "/api/v1/results/events/"
    - from: "tests/results-page.flow.spec.ts"
      to: "app/results/page.tsx"
      via: "rendered-markup assertions for share-link labels and hrefs"
      pattern: "renderToStaticMarkup"
---

<objective>
Expose the new share-image capability from the public results page without adding client-side capture logic or widening the page data flow.

Purpose: Finish Phase 4 at the public UX boundary with a minimal trigger that stays tied to the deterministic route added in 04-01.
Output: `/results` share-image links plus page-flow coverage that locks the route contract in place.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-event-results-share-image/04-RESEARCH.md
@.planning/phases/04-event-results-share-image/04-VALIDATION.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.skills/version-aware-implementation/SKILL.md
@app/results/page.tsx
@app/components/event-participation-list.tsx
@app/components/event-participation-helpers.ts
@tests/results-page.flow.spec.ts
@tests/history-share-image-route.spec.ts

<interfaces>
From `app/components/event-participation-list.tsx` after 04-01:
```ts
type EventParticipationListProps = {
  lang: Language;
  events: EventParticipationCard[];
  emptyMessage: string;
  linkDrivers?: boolean;
  renderEventActions?: (event: EventParticipationCard) => ReactNode;
};
```

From `lib/server/history/types.ts`:
```ts
export type EventParticipationCard = {
  eventId: string;
  seasonYear: number;
  championshipSlug: string;
  championshipName: string;
  roundNumber: number;
  circuitName: string;
  eventDate: string | null;
  participants: EventParticipationEntry[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public share-image trigger to each results event card</name>
  <read_first>
    app/results/page.tsx
    app/components/event-participation-list.tsx
    app/components/event-participation-helpers.ts
    .planning/phases/04-event-results-share-image/04-RESEARCH.md
    .planning/phases/04-event-results-share-image/04-VALIDATION.md
  </read_first>
  <files>app/results/page.tsx</files>
  <behavior>
    - Each rendered event card on `/results` exposes one small link to the public share-image route for that event.
    - The share link uses only the event id plus the current language, not client-side table state or filter copies.
    - English and Spanish visitors see concise bilingual trigger copy.
  </behavior>
  <action>In `app/results/page.tsx`, add a small helper named `buildResultsShareImageHref(eventId, lang)` that returns `/api/v1/results/events/${eventId}/image` for Spanish and `/api/v1/results/events/${eventId}/image?lang=en` for English. Extend the page i18n object with exact copy `shareImage: "Share image"` and `shareImage: "Imagen para compartir"`. Pass `renderEventActions={(event) => (...)}` into `EventParticipationList` so each event header renders a plain `Link` to the deterministic image route. Keep the action minimal and text-only; do not add client-side screenshotting, download libraries, social SDKs, query-string copies of filters, or extra service calls.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts -t share</automated>
  </verify>
  <acceptance_criteria>
    - `app/results/page.tsx` contains `buildResultsShareImageHref`.
    - `app/results/page.tsx` contains `shareImage: "Share image"` and `shareImage: "Imagen para compartir"`.
    - `app/results/page.tsx` contains `/api/v1/results/events/${eventId}/image`.
    - `app/results/page.tsx` passes `renderEventActions` to `EventParticipationList`.
    - `app/results/page.tsx` does not add any import from `next/og` or any new dependency.
  </acceptance_criteria>
  <done>The public results page exposes one deterministic share-image link per event without changing the existing server-first results flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the results-page share trigger and route contract with flow coverage</name>
  <read_first>
    tests/results-page.flow.spec.ts
    app/results/page.tsx
    tests/history-share-image-route.spec.ts
    app/components/event-participation-list.tsx
    .planning/phases/04-event-results-share-image/04-VALIDATION.md
  </read_first>
  <files>tests/results-page.flow.spec.ts</files>
  <behavior>
    - The rendered HTML contains one share-image link per event with the expected route path and language suffix.
    - The share-image link does not remove or break the existing filter-language links or pagination link.
    - The page flow keeps using the event id from the service payload, not a derived slug or round-based route.
  </behavior>
  <action>Extend `tests/results-page.flow.spec.ts` with share-focused cases that render at least two event cards and assert the markup contains `Share image` and `Imagen para compartir` links pointing to `/api/v1/results/events/<eventId>/image` and `/api/v1/results/events/<eventId>/image?lang=en` exactly. Keep the existing pagination and language-switch assertions in place, and add one check that a share link uses the mocked `eventId` even when a championship filter is active. Do not add browser-only assertions; keep this file aligned with the repo’s `renderToStaticMarkup()` flow-test pattern.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts tests/history-share-image-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/results-page.flow.spec.ts` contains `Share image`.
    - `tests/results-page.flow.spec.ts` contains `Imagen para compartir`.
    - `tests/results-page.flow.spec.ts` contains `/api/v1/results/events/event-1/image`.
    - `tests/results-page.flow.spec.ts` contains `/api/v1/results/events/event-1/image?lang=en`.
    - `tests/results-page.flow.spec.ts` still contains an assertion for the existing pagination href with `cursor=`.
  </acceptance_criteria>
  <done>The results-page share trigger is covered by deterministic flow tests, and the public route contract is locked in for both languages.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/results-page.flow.spec.ts tests/history-share-image-route.spec.ts`, then run `npx vitest run tests/history-share-image-route.spec.ts tests/results-page.flow.spec.ts tests/history-api.spec.ts tests/history-api-v2.spec.ts && npm run typecheck` before closing the plan.
</verification>

<success_criteria>
Visitors can trigger one share-image URL per public event card on `/results`, and the page-level contract stays pinned to the canonical event id plus current language.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-results-share-image/04-02-SUMMARY.md`
</output>
