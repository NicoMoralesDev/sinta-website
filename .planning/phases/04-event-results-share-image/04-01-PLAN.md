---
phase: 04-event-results-share-image
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/event-participation-helpers.ts
  - app/components/event-participation-list.tsx
  - lib/server/history/service.ts
  - lib/server/history/repository.ts
  - app/api/v1/results/events/[id]/image/route.ts
  - tests/history-share-image-route.spec.ts
autonomous: true
requirements:
  - SHARE-02
must_haves:
  truths:
    - "A public image route can generate one share image for one specific event id."
    - "The share image uses the same canonical visible columns and points-based participant order as the public `EventParticipationCard` semantics."
    - "Invalid event ids fail with `400`, missing public events fail with `404`, and the route keeps explicit public cache headers."
  artifacts:
    - path: "app/components/event-participation-helpers.ts"
      provides: "Pure shared helpers for event-result columns, values, and headings that both the page card and image route can reuse"
    - path: "app/api/v1/results/events/[id]/image/route.ts"
      provides: "Node-runtime `ImageResponse` endpoint for one public event share image"
    - path: "lib/server/history/service.ts"
      provides: "Validated one-event public read path that returns one `EventParticipationCard` by id"
    - path: "lib/server/history/repository.ts"
      provides: "Repository query that reuses the public results mapping path for one event instead of inventing a second DTO"
    - path: "tests/history-share-image-route.spec.ts"
      provides: "Automated coverage for route validation, cache headers, and parity-critical helper output"
  key_links:
    - from: "app/components/event-participation-list.tsx"
      to: "app/components/event-participation-helpers.ts"
      via: "shared helper imports for session columns and labels"
      pattern: "from \"@/app/components/event-participation-helpers\""
    - from: "app/api/v1/results/events/[id]/image/route.ts"
      to: "lib/server/history/service.ts"
      via: "one-event public read before `ImageResponse` rendering"
      pattern: "getResultsEventParticipationById"
    - from: "lib/server/history/service.ts"
      to: "lib/server/history/repository.ts"
      via: "validated `eventId` lookup"
      pattern: "getEventParticipationCardById"
    - from: "tests/history-share-image-route.spec.ts"
      to: "app/components/event-participation-helpers.ts"
      via: "canonical-column and sparse-column parity assertions"
      pattern: "getEventParticipationSessionColumns|formatEventParticipationSessionValue"
---

<objective>
Build the route-side foundation for Phase 4: one shared event-results interpretation and one deterministic public share-image endpoint.

Purpose: Prevent `/results` and the generated image from drifting apart by centralizing event-participation formatting rules before the public trigger is added.
Output: Shared participation helpers, one-event public read path, image route, and route-focused automated coverage.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-event-results-share-image/04-RESEARCH.md
@.planning/phases/04-event-results-share-image/04-VALIDATION.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.skills/version-aware-implementation/SKILL.md
@app/api/v1/_utils.ts
@app/api/v1/results/events/route.ts
@app/components/event-participation-list.tsx
@app/results/page.tsx
@lib/server/history/service.ts
@lib/server/history/repository.ts
@lib/server/history/types.ts
@tests/history-api.spec.ts

<interfaces>
From `lib/server/history/types.ts`:
```ts
export type EventParticipationEntry = {
  driverSlug: string;
  driverName: string;
  sessions: Array<{
    sessionKind: SessionKind;
    sessionLabel: string;
    position: number | null;
    status: ResultStatus | null;
    rawValue: string;
  }>;
};

export type EventParticipationCard = {
  eventId: string;
  seasonYear: number;
  championshipSlug: string;
  championshipName: string;
  roundNumber: number;
  circuitName: string;
  eventDate: string | null;
  participants: EventParticipationEntry[];
};
```

From `app/components/event-participation-list.tsx`:
```ts
function formatSessionValue(
  session: EventParticipationEntry["sessions"][number],
  lang: Language,
): string;

function getSessionColumns(event: EventParticipationCard): SessionColumn[];

function formatSeasonChampionship(event: EventParticipationCard, lang: Language): string;

function formatRoundCircuit(event: EventParticipationCard): string;
```

From `app/api/v1/admin/events/[id]/results/route.ts`:
```ts
type RouteContext = {
  params: Promise<{ id: string }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the public event-participation helper contract for page and image reuse</name>
  <read_first>
    app/components/event-participation-helpers.ts
    app/components/event-participation-list.tsx
    app/results/page.tsx
    lib/server/history/types.ts
    .planning/phases/04-event-results-share-image/04-RESEARCH.md
    .planning/phases/04-event-results-share-image/04-VALIDATION.md
  </read_first>
  <files>app/components/event-participation-helpers.ts, app/components/event-participation-list.tsx</files>
  <behavior>
    - The page card and the future share-image route use the same helper functions to derive visible columns, session labels, and formatted cell values from one `EventParticipationCard`.
    - Canonical session ordering remains `QS`, `S`, `QF`, `F`, `P`, while sparse historical events only expose stored columns.
    - Normal page JSX stays in `app/components/event-participation-list.tsx`; the share-image route only consumes pure helpers and does not import the component.
  </behavior>
  <action>Create `app/components/event-participation-helpers.ts` and move the pure event-results helpers out of `app/components/event-participation-list.tsx` into named exports with exact names `getEventParticipationSessionOrder`, `getEventParticipationSessionColumns`, `formatEventParticipationSessionValue`, `formatEventParticipationSeasonLabel`, `formatEventParticipationRoundLabel`, and `formatEventParticipationDate`. Update `app/components/event-participation-list.tsx` to import those helpers instead of keeping duplicate local logic. Add an optional prop `renderEventActions?: (event: EventParticipationCard) => ReactNode` and render it in the card header beside the event date, but do not wire any action from `app/results/page.tsx` in this plan. Keep the helper module pure TypeScript with no JSX, no Tailwind classes, and no `next/og` imports.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/event-participation-helpers.ts` contains `export function getEventParticipationSessionColumns`.
    - `app/components/event-participation-helpers.ts` contains `export function formatEventParticipationSessionValue`.
    - `app/components/event-participation-list.tsx` imports from `@/app/components/event-participation-helpers`.
    - `app/components/event-participation-list.tsx` contains `renderEventActions?: (event: EventParticipationCard) => ReactNode`.
    - `app/components/event-participation-list.tsx` no longer contains local function definitions named `getSessionColumns` or `formatSessionValue`.
  </acceptance_criteria>
  <done>A single pure helper module defines the public event-results semantics needed by both the existing card UI and the upcoming image route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the one-event public image route on top of the existing history domain</name>
  <read_first>
    app/api/v1/results/events/[id]/image/route.ts
    tests/history-share-image-route.spec.ts
    app/api/v1/_utils.ts
    app/api/v1/results/events/route.ts
    app/components/event-participation-helpers.ts
    lib/server/history/service.ts
    lib/server/history/repository.ts
    lib/server/history/types.ts
    .planning/phases/04-event-results-share-image/04-RESEARCH.md
    .planning/phases/04-event-results-share-image/04-VALIDATION.md
  </read_first>
  <files>lib/server/history/service.ts, lib/server/history/repository.ts, app/api/v1/results/events/[id]/image/route.ts, tests/history-share-image-route.spec.ts</files>
  <behavior>
    - `GET /api/v1/results/events/{id}/image` returns a generated image for one public event using the same visible columns and participant order as the `EventParticipationCard`.
    - An invalid `id` returns `400`, a missing public event returns `404`, and the route keeps `Cache-Control: public, s-maxage=120, stale-while-revalidate=600`.
    - The image composition stays separate from page JSX and uses inline `ImageResponse` styles only.
    - Dense event tables compute a taller image height so every participant row fits without clipping.
  </behavior>
  <action>In `lib/server/history/repository.ts`, add `getEventParticipationCardById(eventId: string): Promise<EventParticipationCard | null>` by querying one active event plus its active result rows, then reusing the existing `toEventResultItems(...)` and `toParticipationCards(...)` mapping path instead of introducing a second event-results transformation. In `lib/server/history/service.ts`, add `getResultsEventParticipationById(eventId: string): Promise<EventParticipationCard>` that validates the UUID with the existing `parseUuid(...)`, throws `HistoryValidationError(\"eventId must be a UUID.\")` for bad ids, and throws `HistoryNotFoundError` when the repository returns `null`. Create `app/api/v1/results/events/[id]/image/route.ts` with `export const runtime = \"nodejs\"`, a `RouteContext` matching the repo’s dynamic-route pattern, and a `GET` handler that reads `params.id`, resolves `lang` from the query string, fetches the event card, derives columns through `getEventParticipationSessionColumns(...)`, computes `const imageHeight = Math.max(1350, 420 + event.participants.length * 72)`, and passes that height into `new ImageResponse(...)` so dense tables cannot clip lower rows. Keep the image JSX route-local and inline-styled; do not import `EventParticipationList`, Tailwind classes, or new dependencies. Add `tests/history-share-image-route.spec.ts` to cover 200/400/404 responses, exact cache headers, helper-driven parity checks for `QS`, `S`, `QF`, `F`, `P`, sparse historical omission, service-order preservation, and one dense-event fixture that asserts the computed image height grows beyond `1350` when many participants are present.</action>
  <verify>
    <automated>npx vitest run tests/history-share-image-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/server/history/repository.ts` contains `export async function getEventParticipationCardById`.
    - `lib/server/history/service.ts` contains `export async function getResultsEventParticipationById`.
    - `app/api/v1/results/events/[id]/image/route.ts` contains `export const runtime = "nodejs"`.
    - `app/api/v1/results/events/[id]/image/route.ts` contains `new ImageResponse(`.
    - `app/api/v1/results/events/[id]/image/route.ts` contains `Math.max(1350, 420 + event.participants.length * 72)`.
    - `tests/history-share-image-route.spec.ts` contains `public, s-maxage=120, stale-while-revalidate=600`.
    - `tests/history-share-image-route.spec.ts` contains assertions for `QS`, `S`, `QF`, `F`, `P` and one sparse event case that excludes `QS`, `QF`, or `P`.
    - `tests/history-share-image-route.spec.ts` contains one dense-event assertion equivalent to `expect(imageHeight).toBeGreaterThan(1350)`.
  </acceptance_criteria>
  <done>The repo exposes a deterministic public event-share image route that reuses the same event-participation semantics as the public results page.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/history-share-image-route.spec.ts` after the route work, then run `npx vitest run tests/results-page.flow.spec.ts tests/history-share-image-route.spec.ts tests/history-api.spec.ts tests/history-api-v2.spec.ts && npm run typecheck` before closing the plan.
</verification>

<success_criteria>
Phase 4 has a route-level foundation: one event id maps to one public share image, and the image’s columns and participant order are derived from the same canonical helpers as the public event card.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-results-share-image/04-01-SUMMARY.md`
</output>
