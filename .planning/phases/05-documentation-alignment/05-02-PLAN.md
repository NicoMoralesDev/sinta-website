---
phase: 05-documentation-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-dashboard.md
  - docs/data-import.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "A maintainer can follow the admin documentation and understand the real five-column results editor, organizer workflow, dry-run behavior, and cache policy."
    - "A maintainer can follow the import documentation and understand the workbook prerequisite, idempotent import commands, and why parser and DB verification are limited on a fresh checkout."
    - "The admin and import docs describe the shipped workflow instead of the older two-session or clean-full-suite assumptions."
  artifacts:
    - path: "docs/admin-dashboard.md"
      provides: "Accurate admin workflow documentation for auth, canonical results editing, organizer metadata, dry-run semantics, and no-store APIs"
    - path: "docs/data-import.md"
      provides: "Accurate import and verification guidance for the workbook-based history import and known test caveats"
  key_links:
    - from: "docs/admin-dashboard.md"
      to: "app/admin/_components/events-manager.tsx"
      via: "documented canonical grid order and validation rules"
      pattern: "fieldOrder|Points accepts whole numbers"
    - from: "docs/admin-dashboard.md"
      to: "app/admin/_components/championships-manager.tsx"
      via: "documented optional organizer payload behavior"
      pattern: "organizerName"
    - from: "docs/data-import.md"
      to: "tests/history-parser.spec.ts"
      via: "documented workbook prerequisite and fresh-checkout limitation"
      pattern: "Historia The New Project.xlsx"
---

<objective>
Align the detailed admin and import docs with the runtime behavior shipped in Phases 1 through 4.

Purpose: The root README should stay concise, so the deeper workflow docs need to carry the precise admin-grid, organizer, import, and verification-limit details maintainers will actually use.
Output: Updated `docs/admin-dashboard.md` and `docs/data-import.md` that reflect the current UI, API behavior, and known test constraints.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-documentation-alignment/05-RESEARCH.md
@.planning/phases/05-documentation-alignment/05-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-02-SUMMARY.md
@.planning/phases/02-admin-results-and-championship-editing/02-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.skills/planning-and-verification/SKILL.md
@.skills/version-aware-implementation/SKILL.md
@docs/admin-dashboard.md
@docs/data-import.md
@app/admin/_components/events-manager.tsx
@app/admin/_components/championships-manager.tsx
@app/api/v1/admin/_utils.ts
@scripts/import-results-xlsx.ts
@tests/admin-events-manager.flow.spec.ts
@tests/admin-championships-manager.flow.spec.ts
@tests/history-parser.spec.ts
@tests/db.integration.spec.ts

<interfaces>
From `tests/admin-events-manager.flow.spec.ts`:
```ts
fieldOrder: ["qs", "s", "qf", "f", "p"],
fieldLabels: {
  qs: "Qualy Sprint",
  s: "Sprint",
  qf: "Qualy Final",
  f: "Final",
  p: "Points",
}
```

From `app/admin/_components/events-manager.tsx`:
```ts
<p>Race fields accept positive integers or DNF / DNQ / DSQ / ABSENT.</p>
<p>Points accepts whole numbers &gt;= 0.</p>
```

From `app/admin/_components/championships-manager.tsx`:
```ts
organizerName: normalizeOptionalText(String(formData.get("organizerName") ?? "")),
organizerName: normalizeOptionalText(draft.organizerName),
```

From `app/api/v1/admin/_utils.ts`:
```ts
const ADMIN_CACHE_CONTROL = "no-store";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `docs/admin-dashboard.md` for the canonical results editor and organizer workflow</name>
  <read_first>
    docs/admin-dashboard.md
    app/admin/_components/events-manager.tsx
    app/admin/_components/championships-manager.tsx
    app/api/v1/admin/_utils.ts
    tests/admin-events-manager.flow.spec.ts
    tests/admin-championships-manager.flow.spec.ts
    .planning/phases/05-documentation-alignment/05-RESEARCH.md
  </read_first>
  <files>docs/admin-dashboard.md</files>
  <action>Rewrite `docs/admin-dashboard.md` in English and keep it scoped to the real admin workflows. Preserve the existing auth/bootstrap/dry-run coverage, then add sections named exactly `Canonical event results editor` and `Championship organizer metadata`. In the results-editor section, document that the grid is driven by backend `fieldOrder` / `fieldLabels`, the canonical fields are `QS`, `S`, `QF`, `F`, `P`, race cells accept positive integers or `DNF` / `DNQ` / `DSQ` / `ABSENT`, points accept whole numbers `>= 0`, and partial saves preserve untouched persisted cells while explicit clears serialize as tombstones rather than wiping the whole row. In the organizer section, document `organizerName` as an optional trimmed championship field that is sent on both create and update payloads, with blank input normalized to `null`. Keep the admin API note that responses are `Cache-Control: no-store`, and explain that development writes return preview-style success with `dryRun: true` when `ADMIN_DEV_DRY_RUN=1`. Remove any lingering implication that the admin model only has two session columns.</action>
  <verify>
    <automated>npx vitest run tests/admin-events-manager.flow.spec.ts tests/admin-championships-manager.flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/admin-dashboard.md` contains `## Canonical event results editor`.
    - `docs/admin-dashboard.md` contains `QS`, `S`, `QF`, `F`, and `P`.
    - `docs/admin-dashboard.md` contains `DNF`, `DNQ`, `DSQ`, and `ABSENT`.
    - `docs/admin-dashboard.md` contains `Points accept whole numbers >= 0`.
    - `docs/admin-dashboard.md` contains `fieldOrder` and `fieldLabels`.
    - `docs/admin-dashboard.md` contains `organizerName`.
    - `docs/admin-dashboard.md` contains `dryRun: true`.
    - `docs/admin-dashboard.md` contains `Cache-Control: no-store`.
  </acceptance_criteria>
  <done>The admin workflow doc matches the shipped five-column results editor, organizer handling, and development dry-run semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Update `docs/data-import.md` for workbook prerequisites and honest verification limits</name>
  <read_first>
    docs/data-import.md
    scripts/import-results-xlsx.ts
    tests/history-parser.spec.ts
    tests/db.integration.spec.ts
    .planning/phases/05-documentation-alignment/05-RESEARCH.md
    .planning/phases/05-documentation-alignment/05-VALIDATION.md
    .planning/codebase/TESTING.md
  </read_first>
  <files>docs/data-import.md</files>
  <action>Rewrite `docs/data-import.md` in English and keep it focused on the existing manual workbook flow. Document the exact workbook path `data-source/Historia The New Project.xlsx`, the commands `npm run import:results -- --dry-run` and `npm run import:results -- --apply`, and that the import remains idempotent. Add a section named exactly `Verification limits` that states: `tests/history-parser.spec.ts` depends on that local workbook and therefore fails on a fresh checkout without the file; `npm run test:db` only works with a valid `.env`, network access, and the wrapper that enables `RUN_DB_INTEGRATION_TESTS=1`; and maintainers should treat the focused Vitest subset plus manual doc review as the reliable Phase 5 gate. If the current doc implies a clean full-suite fresh-checkout path, remove that implication. Do not add any future authenticated upload workflow beyond a short deferred note.</action>
  <verify>
    <automated>rg -n "Historia The New Project.xlsx|npm run import:results -- --dry-run|npm run import:results -- --apply|Verification limits|tests/history-parser.spec.ts|npm run test:db|RUN_DB_INTEGRATION_TESTS=1|fresh checkout" docs/data-import.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/data-import.md` contains `data-source/Historia The New Project.xlsx`.
    - `docs/data-import.md` contains `npm run import:results -- --dry-run`.
    - `docs/data-import.md` contains `npm run import:results -- --apply`.
    - `docs/data-import.md` contains `## Verification limits`.
    - `docs/data-import.md` contains `tests/history-parser.spec.ts`.
    - `docs/data-import.md` contains `fresh checkout`.
    - `docs/data-import.md` contains `npm run test:db`.
    - `docs/data-import.md` contains `RUN_DB_INTEGRATION_TESTS=1`.
  </acceptance_criteria>
  <done>The import doc tells maintainers the exact workbook and command prerequisites, and it documents the real parser and DB verification constraints instead of overstating test coverage.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/admin-events-manager.flow.spec.ts tests/admin-championships-manager.flow.spec.ts`, then run `rg -n "Verification limits|Historia The New Project.xlsx|RUN_DB_INTEGRATION_TESTS=1|dryRun: true|Cache-Control: no-store" docs/admin-dashboard.md docs/data-import.md` so the updated docs are both behavior-backed and grep-verifiable.
</verification>

<success_criteria>
Maintainers can read the detailed admin and import docs and get an accurate picture of the five-column results editor, organizer metadata workflow, workbook-driven import path, and the real verification constraints for this repo.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-alignment/05-02-SUMMARY.md`
</output>
