---
phase: 05-documentation-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/results-model.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "A maintainer can open `README.md` and find the current runtime, cache model, primary scripts, migration baseline, and the right follow-up docs without relying on stale Spanish prose."
    - "A maintainer can open one focused results-model document and see the canonical `QS` / `S` / `QF` / `F` / `P` contract, points-first ordering, organizer metadata, and the event share-image route examples."
    - "A maintainer can see the real verification limits for this milestone instead of being told the full fresh-checkout suite is clean."
  artifacts:
    - path: "README.md"
      provides: "English maintainer index for runtime setup, workflows, cache semantics, migrations, and verification guidance"
    - path: "docs/results-model.md"
      provides: "Focused documentation for canonical results behavior, public ordering, organizer metadata, share-image flow, and known verification caveats"
  key_links:
    - from: "README.md"
      to: "docs/results-model.md"
      via: "maintainer workflow index link"
      pattern: "docs/results-model.md"
    - from: "docs/results-model.md"
      to: "lib/server/history/types.ts"
      via: "documented canonical results contract"
      pattern: "CANONICAL_RESULT_FIELDS"
    - from: "docs/results-model.md"
      to: "app/results/page.tsx"
      via: "documented share-image route and organizer behavior"
      pattern: "buildResultsShareImageHref|organizerLabel"
---

<objective>
Refresh the maintainer entrypoint docs so they match the shipped runtime and Phase 1-4 results behavior.

Purpose: README should become the reliable starting point for maintainers, while one focused results-model document carries the milestone-specific contract details that do not belong inline in the root index.
Output: An English README index plus a new `docs/results-model.md` that documents the canonical results model, ordering, sharing, and verification limits.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-documentation-alignment/05-RESEARCH.md
@.planning/phases/05-documentation-alignment/05-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-03-SUMMARY.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-01-SUMMARY.md
@.planning/phases/04-event-results-share-image/04-03-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.skills/planning-and-verification/SKILL.md
@.skills/version-aware-implementation/SKILL.md
@README.md
@package.json
@lib/server/history/types.ts
@app/results/page.tsx
@app/api/v1/_utils.ts
@app/api/v1/admin/_utils.ts
@app/api/v1/results/events/[id]/image/route.ts
@tests/history-share-image-route.spec.ts
@tests/results-page.flow.spec.ts
@tests/history-repository.spec.ts
@tests/championship-organizer.spec.ts

<interfaces>
From `package.json`:
```json
{
  "engines": { "node": "22.x" },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:db": "node --env-file=.env scripts/run-db-test.mjs",
    "import:results": "node ... scripts/import-results-xlsx.ts",
    "bootstrap:admin": "node ... scripts/bootstrap-admin-owner.ts"
  }
}
```

From `lib/server/history/types.ts`:
```ts
export const CANONICAL_RESULT_FIELDS = ["qs", "s", "qf", "f", "p"] as const;

export type ChampionshipFilter = {
  id: string;
  seasonYear: number;
  slug: string;
  name: string;
  organizerName: string | null;
};
```

From `app/results/page.tsx` and the API helpers:
```ts
export const revalidate = 120;

function buildResultsShareImageHref(
  eventId: string,
  lang: "es" | "en",
  driver?: string,
): string;

const DEFAULT_CACHE_CONTROL = "public, s-maxage=120, stale-while-revalidate=600";
const ADMIN_CACHE_CONTROL = "no-store";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `README.md` as the English maintainer index for runtime and workflows</name>
  <read_first>
    README.md
    package.json
    .planning/phases/05-documentation-alignment/05-RESEARCH.md
    .planning/phases/05-documentation-alignment/05-VALIDATION.md
    .planning/codebase/STACK.md
    app/results/page.tsx
    app/api/v1/_utils.ts
    app/api/v1/admin/_utils.ts
  </read_first>
  <files>README.md</files>
  <action>Replace the current mixed-language README with a concise English maintainer index. Keep it short and practical. Add sections named exactly `Runtime`, `Local setup`, `Verification`, `Workflow docs`, and `Operational notes`. In `Runtime`, state Node.js `22.x`, npm, Next.js `16.1.6`, React `19.2.4`, and that public pages plus public APIs use 120-second caching (`revalidate = 120` / `Cache-Control: public, s-maxage=120, stale-while-revalidate=600`) while admin APIs are `Cache-Control: no-store`. In `Local setup`, keep `.env` creation, `DATABASE_URL`, the `ADMIN_*` variables, and the primary scripts `npm run dev`, `npm run build`, `npm run start`, `npm run import:results`, and `npm run bootstrap:admin`. In `Verification`, list `npm run lint`, `npm run typecheck`, the focused Vitest command from `05-VALIDATION.md`, and one explicit caveat that `npm run test` is not a clean fresh-checkout gate because `tests/history-parser.spec.ts` depends on `data-source/Historia The New Project.xlsx`. In `Workflow docs`, link to `docs/results-model.md`, `docs/admin-dashboard.md`, and `docs/data-import.md`. In `Operational notes`, mention migrations through `009_canonical_results_contract.sql`, the public `/results` share-image route family, and the admin dry-run default in development. Do not turn README into a full route-by-route manual and do not keep Spanish narrative text.</action>
  <verify>
    <automated>rg -n "Runtime|Verification|Workflow docs|docs/results-model.md|009_canonical_results_contract.sql|public, s-maxage=120, stale-while-revalidate=600|no-store|tests/history-parser.spec.ts" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a `## Runtime` heading.
    - `README.md` contains `Node.js \`22.x\``.
    - `README.md` contains `public, s-maxage=120, stale-while-revalidate=600`.
    - `README.md` contains `no-store`.
    - `README.md` contains `docs/results-model.md`.
    - `README.md` contains `009_canonical_results_contract.sql`.
    - `README.md` contains `tests/history-parser.spec.ts`.
    - `README.md` no longer contains the Spanish line `Sitio web de SINTA eSports construido con Next.js`.
  </acceptance_criteria>
  <done>`README.md` becomes the repo’s reliable English entrypoint for setup, runtime behavior, verification expectations, and follow-up workflow docs.</done>
</task>

<task type="auto">
  <name>Task 2: Add `docs/results-model.md` for the canonical results and sharing contract</name>
  <read_first>
    docs/results-model.md
    lib/server/history/types.ts
    app/results/page.tsx
    app/api/v1/results/events/[id]/image/route.ts
    tests/results-page.flow.spec.ts
    tests/history-share-image-route.spec.ts
    tests/history-repository.spec.ts
    tests/championship-organizer.spec.ts
    .planning/phases/05-documentation-alignment/05-RESEARCH.md
    .planning/phases/05-documentation-alignment/05-VALIDATION.md
  </read_first>
  <files>docs/results-model.md</files>
  <action>Create a new English document at `docs/results-model.md` with sections named exactly `Canonical fields`, `Display and ordering rules`, `Organizer metadata`, `Share-image route`, and `Verification limits`. In `Canonical fields`, document the stored field order as `qs`, `s`, `qf`, `f`, `p` and explain that public/admin surfaces show `QS`, `S`, `QF`, `F`, `P`; also call out that championships still control the display labels for sprint and final. In `Display and ordering rules`, state that participants are ordered by points first, then final-race position and driver name for legacy rows, and that sparse historical events omit missing columns instead of fabricating them. In `Organizer metadata`, describe `organizerName` as optional championship-level metadata that renders in the `/results` championship context. In `Share-image route`, document the exact route contract `/api/v1/results/events/:eventId/image`, the optional `driver=<slug>` filter, the optional `lang=en` query, and that the image uses the same visible columns and participant set as the public event card. In `Verification limits`, document the fresh-checkout parser-test limitation, the environment-dependent `npm run test:db` conditions, and that visual/image correctness still needs manual review even when the focused Vitest suite passes. Do not add future-scope sharing features or direct social integrations.</action>
  <verify>
    <automated>rg -n "Canonical fields|Display and ordering rules|Organizer metadata|Share-image route|Verification limits|qs, s, qf, f, p|points first|/api/v1/results/events/:eventId/image|driver=<slug>|lang=en|tests/history-parser.spec.ts|npm run test:db" docs/results-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/results-model.md` contains `## Canonical fields`.
    - `docs/results-model.md` contains `qs, s, qf, f, p`.
    - `docs/results-model.md` contains `points first`.
    - `docs/results-model.md` contains `organizerName`.
    - `docs/results-model.md` contains `/api/v1/results/events/:eventId/image`.
    - `docs/results-model.md` contains `driver=<slug>`.
    - `docs/results-model.md` contains `lang=en`.
    - `docs/results-model.md` contains `tests/history-parser.spec.ts`.
    - `docs/results-model.md` contains `npm run test:db`.
  </acceptance_criteria>
  <done>There is one focused doc that explains the milestone’s results and share-image behavior, plus the verification caveats maintainers need to know.</done>
</task>

</tasks>

<verification>
Run `rg -n "Runtime|Verification|docs/results-model.md|009_canonical_results_contract.sql" README.md docs/results-model.md`, then run `npx vitest run tests/results-page.flow.spec.ts tests/history-share-image-route.spec.ts tests/championship-organizer.spec.ts tests/history-repository.spec.ts && npm run typecheck && npm run lint` so the documented behavior is still backed by current source and tests.
</verification>

<success_criteria>
Maintainers can start from `README.md`, jump into one dedicated results-model doc, and find accurate guidance for the canonical results contract, share-image route, and real verification limits without relying on stale or mixed-language docs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-alignment/05-01-SUMMARY.md`
</output>
