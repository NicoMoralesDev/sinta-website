---
phase: 03-public-results-organizer-display-and-driver-stats-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drivers/[slug]/page.tsx
  - tests/driver-profile-page.flow.spec.ts
  - tests/driver-visualizations.spec.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
must_haves:
  truths:
    - "The recent-position sparkline uses one race result per event, so qualifying, sprint, and points rows do not create fake performance trend points."
    - "The round heatmap shows the final-race outcome for each event instead of the best cross-session result."
    - "Better finishes render higher on the sparkline because lower position numbers map to smaller SVG `y` values."
  artifacts:
    - path: "app/drivers/[slug]/page.tsx"
      provides: "Final-race-only derivation for `numericPositions` and `heatmapItems` while keeping the visible history table unchanged"
    - path: "tests/driver-profile-page.flow.spec.ts"
      provides: "Mixed-session driver-page regression coverage for race-only chart and heatmap inputs"
    - path: "tests/driver-visualizations.spec.ts"
      provides: "Direct sparkline orientation coverage for STAT-01"
  key_links:
    - from: "app/drivers/[slug]/page.tsx"
      to: "SparklinePositions"
      via: "final-race-only `numericPositions` prop"
      pattern: "numericPositions"
    - from: "app/drivers/[slug]/page.tsx"
      to: "RoundHeatmap"
      via: "final-race-only `heatmapItems` prop"
      pattern: "heatmapItems"
    - from: "tests/driver-visualizations.spec.ts"
      to: "app/components/visualizations.tsx"
      via: "polyline coordinate assertions for lower-position-is-higher rendering"
      pattern: "polyline|points"
---

<objective>
Make the public driver profile charts race-correct after the canonical `QS` / `S` / `QF` / `F` / `P` expansion, and prove the sparkline orientation explicitly.

Purpose: Fix the remaining public stats correctness bug at the page layer without reopening repository aggregates that are already race-only.
Output: Final-race-only driver trend and heatmap derivation plus direct sparkline-orientation coverage.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-03-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.skills/version-aware-implementation/SKILL.md
@app/drivers/[slug]/page.tsx
@app/components/visualizations.tsx
@lib/server/history/types.ts
@tests/driver-profile-page.flow.spec.ts

<interfaces>
From `lib/server/history/types.ts`:
```ts
export type EventResultEntry = {
  driverSlug: string;
  driverName: string;
  sessionKind: SessionKind;
  sessionLabel: string;
  position: number | null;
  status: ResultStatus | null;
  rawValue: string;
};

export type EventResultItem = {
  eventId: string;
  seasonYear: number;
  championshipSlug: string;
  championshipName: string;
  roundNumber: number;
  circuitName: string;
  eventDate: string | null;
  results: EventResultEntry[];
};
```

From `app/components/visualizations.tsx`:
```ts
export function SparklinePositions({ positions, label, lang = "en" }: SparklinePositionsProps)
export function RoundHeatmap({ title, items, lang = "en" }: RoundHeatmapProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restrict driver trend and heatmap derivation to final-race results</name>
  <read_first>
    app/drivers/[slug]/page.tsx
    tests/driver-profile-page.flow.spec.ts
    lib/server/history/types.ts
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-VALIDATION.md
  </read_first>
  <files>app/drivers/[slug]/page.tsx, tests/driver-profile-page.flow.spec.ts</files>
  <behavior>
    - For an event containing `qs`, `s`, `qf`, `f`, and `p`, the driver trend uses only the `f` result, or the legacy `secondary` alias when present.
    - Events without an `f` or `secondary` result do not produce a sparkline point or heatmap value.
    - The stats cards keep using `profile.stats` from the repository and are not recalculated in the page.
  </behavior>
  <action>In `app/drivers/[slug]/page.tsx`, add a small page-local helper such as `getRaceResult(event)` that returns the first result whose `sessionKind` is `"f"` or `"secondary"`. Use that helper for both `numericPositions` and `heatmapItems`; stop flattening all sessions and stop picking `bestSession` across every stored result. Leave `historyCards` unchanged so the visible event history table still shows all stored sessions. Extend `tests/driver-profile-page.flow.spec.ts` with mixed-session events containing `qs`, `s`, `qf`, `f`, and `p`, then assert the rendered sparkline summary text and `RoundHeatmap` aria labels reflect only the final-race result per event.</action>
  <verify>
    <automated>npx vitest run tests/driver-profile-page.flow.spec.ts tests/history-repository.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/drivers/[slug]/page.tsx` contains a helper or inline filter that matches `sessionKind === "f" || sessionKind === "secondary"`.
    - `app/drivers/[slug]/page.tsx` no longer computes `numericPositions` by flattening every result from every event.
    - `app/drivers/[slug]/page.tsx` no longer derives `heatmapItems` from the best position across all sessions.
    - `tests/driver-profile-page.flow.spec.ts` includes mixed canonical-session fixtures with `qs`, `s`, `qf`, `f`, and `p`.
    - `tests/driver-profile-page.flow.spec.ts` asserts at least one heatmap `aria-label` or title string built from the final-race result only.
  </acceptance_criteria>
  <done>The driver profile charts consume one race result per event, keeping trend and heatmap data race-correct after the canonical results expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit sparkline-orientation coverage</name>
  <read_first>
    app/components/visualizations.tsx
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-VALIDATION.md
  </read_first>
  <files>tests/driver-visualizations.spec.ts</files>
  <behavior>
    - `SparklinePositions` renders `P1` higher than `P5` by producing a smaller SVG `y` coordinate for the better finish.
    - `SparklinePositions` still renders the empty-state copy when `positions` is empty.
  </behavior>
  <action>Create `tests/driver-visualizations.spec.ts` as the missing Wave 0 spec from validation. Render `SparklinePositions` with `renderToStaticMarkup()`, extract the `<polyline points="...">` attribute, parse the first and later `y` coordinates, and assert the better finish has the smaller numeric `y` value. Add one empty-state assertion in both English or Spanish copy so STAT-01 is covered directly without changing `app/components/visualizations.tsx` unless the test exposes a real bug.</action>
  <verify>
    <automated>npx vitest run tests/driver-visualizations.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/driver-visualizations.spec.ts` imports `SparklinePositions` from `@/app/components/visualizations`.
    - `tests/driver-visualizations.spec.ts` parses the rendered `polyline` `points` attribute.
    - `tests/driver-visualizations.spec.ts` contains an assertion equivalent to `expect(bestFinishY).toBeLessThan(worseFinishY)`.
    - `tests/driver-visualizations.spec.ts` contains one empty-state assertion for `No numeric positions available.` or `No hay posiciones numéricas disponibles.`.
  </acceptance_criteria>
  <done>The sparkline orientation is enforced by an automated spec instead of relying on visual assumption.</done>
</task>

</tasks>

<verification>
Run the driver page flow spec, the new visualizations spec, and the existing history repository spec. Confirm the page now filters to final-race rows locally while repository aggregates remain unchanged.
</verification>

<success_criteria>
Driver trend and heatmap inputs stay race-correct with canonical session data, and the sparkline orientation is covered by an explicit automated test.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-02-SUMMARY.md`
</output>
