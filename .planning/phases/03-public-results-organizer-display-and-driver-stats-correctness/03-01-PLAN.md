---
phase: 03-public-results-organizer-display-and-driver-stats-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/results/page.tsx
  - tests/results-page.flow.spec.ts
autonomous: true
requirements:
  - RESULT-01
  - RESULT-02
  - RESULT-03
  - RESULT-04
must_haves:
  truths:
    - "Visitors see organizer text inline with the public championship context on `/results` whenever organizer metadata exists."
    - "Public event tables continue to render result columns only for stored sessions, in canonical order `QS`, `S`, `QF`, `F`, `P`."
    - "The results page preserves the service/repository participant order instead of re-sorting rows in the UI."
  artifacts:
    - path: "app/results/page.tsx"
      provides: "Selected/current championship organizer rendering without changing the existing server-first results flow"
    - path: "tests/results-page.flow.spec.ts"
      provides: "Regression coverage for organizer display, canonical session ordering, sparse historical columns, and render-order preservation"
  key_links:
    - from: "app/results/page.tsx"
      to: "filters.championships"
      via: "selected championship lookup for organizerName"
      pattern: "filters\\?\\.championships"
    - from: "app/results/page.tsx"
      to: "current.championship.organizerName"
      via: "fallback organizer rendering in the current championship card"
      pattern: "current\\.championship\\.organizerName"
    - from: "tests/results-page.flow.spec.ts"
      to: "app/results/page.tsx"
      via: "static markup assertions for organizer copy and event table rendering"
      pattern: "renderToStaticMarkup"
---

<objective>
Keep the public results hub trustworthy after the canonical results expansion, while finally surfacing championship organizer metadata.

Purpose: Preserve the Phase 1 public-result semantics already implemented in shared reads, and add the missing organizer display with minimal UI surface area.
Output: Organizer-aware `/results` rendering plus regression coverage for canonical order, sparse historical columns, and UI order preservation.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
@.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-03-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.skills/version-aware-implementation/SKILL.md
@app/results/page.tsx
@app/components/event-participation-list.tsx
@lib/server/history/types.ts
@tests/results-page.flow.spec.ts
@tests/history-api.spec.ts
@tests/history-api-v2.spec.ts

<interfaces>
From `lib/server/history/types.ts`:
```ts
export type ChampionshipFilter = {
  id: string;
  seasonYear: number;
  slug: string;
  name: string;
  organizerName: string | null;
};

export type CurrentChampionshipSummary = {
  championship: {
    id: string;
    seasonYear: number;
    slug: string;
    name: string;
    organizerName: string | null;
  };
  events: EventParticipationCard[];
  leaderboard: Array<{
    driverSlug: string;
    driverName: string;
    wins: number;
    podiums: number;
    top10: number;
    completed: number;
    avgPosition: number | null;
  }>;
};

export type EventParticipationCard = {
  eventId: string;
  seasonYear: number;
  championshipSlug: string;
  championshipName: string;
  roundNumber: number;
  circuitName: string;
  eventDate: string | null;
  participants: EventParticipationEntry[];
};
```

From `app/components/event-participation-list.tsx`:
```ts
function getSessionColumns(event: EventParticipationCard): SessionColumn[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render organizer metadata in the public results championship context</name>
  <read_first>
    app/results/page.tsx
    tests/results-page.flow.spec.ts
    lib/server/history/types.ts
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-VALIDATION.md
  </read_first>
  <files>app/results/page.tsx, tests/results-page.flow.spec.ts</files>
  <behavior>
    - When a selected championship in `filters.championships` has `organizerName`, `/results` renders `Organizer: {name}` in English and `Organizador: {name}` in Spanish.
    - When no championship filter is active, the current championship card renders organizer text from `current.championship.organizerName`.
    - Organizer text is omitted when `organizerName` is `null` or blank.
  </behavior>
  <action>Add the smallest possible organizer presentation to `app/results/page.tsx`. Reuse the already-loaded `filters.championships` and `current.championship` payloads; do not widen DTOs or add new fetches. Derive `selectedChampionship` from `selectedChampionshipId` first, then fall back to slug/year matching only when needed. Add bilingual copy keys `organizerLabel: "Organizer"` and `organizerLabel: "Organizador"`. Render organizer text inline with the results-page championship context, preferring the selected championship metadata when present and falling back to the existing current championship section otherwise. Extend `tests/results-page.flow.spec.ts` to assert the exact organizer strings are present for both selected and current championship scenarios, and to assert organizer text is absent when the payload provides `organizerName: null`.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts -t organizer</automated>
  </verify>
  <acceptance_criteria>
    - `app/results/page.tsx` contains `organizerLabel: "Organizer"` and `organizerLabel: "Organizador"`.
    - `app/results/page.tsx` reads organizer data from `filters?.championships` or `current.championship.organizerName` without adding a new service call.
    - `tests/results-page.flow.spec.ts` contains assertions for `Organizer: SINTA` and `Organizador: SINTA`.
    - `tests/results-page.flow.spec.ts` contains one assertion that organizer text is not rendered when `organizerName` is `null`.
  </acceptance_criteria>
  <done>The results hub shows organizer metadata in public championship context using the existing server payloads only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock public results rendering to the existing canonical and sparse-column behavior</name>
  <read_first>
    tests/results-page.flow.spec.ts
    app/results/page.tsx
    app/components/event-participation-list.tsx
    tests/history-api.spec.ts
    tests/history-api-v2.spec.ts
    .planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-RESEARCH.md
  </read_first>
  <files>tests/results-page.flow.spec.ts</files>
  <behavior>
    - A public event table renders headers in the order `QS`, `S`, `QF`, `F`, `P` when those sessions exist.
    - A sparse historical event only renders the stored session headers and never inserts empty `QS`, `QF`, or `P` columns.
    - Driver row order in rendered markup matches the order returned by the service, so points-first sorting from the repository is preserved in the UI.
  </behavior>
  <action>Extend `tests/results-page.flow.spec.ts` with one explicit render-order regression: pass a mocked event whose `participants` array is already points-first and assert the HTML keeps that order instead of re-sorting by final-race position or driver name. Keep the existing canonical header-order and sparse historical assertions, but tighten them so the spec remains the executable contract for RESULT-01 through RESULT-03. Do not replace `EventParticipationList` column derivation with a fixed five-column header anywhere in this plan.</action>
  <verify>
    <automated>npx vitest run tests/results-page.flow.spec.ts tests/history-api.spec.ts tests/history-api-v2.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/results-page.flow.spec.ts` contains a participant-order assertion using `indexOf(...)` or equivalent rendered-markup order checks.
    - `tests/results-page.flow.spec.ts` still asserts `QS`, `S`, `QF`, `F`, `P` header order.
    - `tests/results-page.flow.spec.ts` still asserts the sparse historical event section does not contain `>QS<`, `>QF<`, or `>P<`.
    - No file in this plan introduces a fixed always-on five-column public results header.
  </acceptance_criteria>
  <done>The results page has executable regression coverage for canonical session order, sparse historical columns, organizer rendering, and service-order preservation.</done>
</task>

</tasks>

<verification>
Run the results-page flow spec plus the existing results API specs. Confirm organizer rendering is present only when organizer metadata exists, and confirm the public table still relies on the existing sparse column derivation.
</verification>

<success_criteria>
`/results` exposes organizer metadata without disturbing canonical column ordering, points-first public ordering, or sparse historical event rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-results-organizer-display-and-driver-stats-correctness/03-01-SUMMARY.md`
</output>
