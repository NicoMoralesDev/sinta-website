---
phase: 01-results-contract-ordering-and-championship-metadata
plan: 02
type: execute
wave: 3
depends_on:
  - 01-00
  - 01-01
files_modified:
  - lib/server/admin/repository.ts
  - lib/server/admin/service.ts
  - app/api/v1/admin/events/[id]/results/route.ts
  - app/api/v1/admin/championships/route.ts
  - tests/admin-event-results-contract.spec.ts
  - tests/admin-event-results-preserve.spec.ts
  - tests/championship-organizer.spec.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
must_haves:
  truths:
    - "Admin writes can persist canonical result fields without dropping omitted historical or canonical rows during Phase 1."
    - "The unchanged two-column admin editor remains backward compatible by sending legacy `primary` and `secondary` values that normalize to canonical storage."
    - "Championship organizer metadata can be created, updated, and listed through the existing admin service and API route."
  artifacts:
    - path: "lib/server/admin/service.ts"
      provides: "Canonical result normalization, preserve-on-save behavior, and organizer validation"
    - path: "lib/server/admin/repository.ts"
      provides: "Merged event-result persistence and organizer column reads/writes"
    - path: "tests/admin-event-results-preserve.spec.ts"
      provides: "Regression coverage for omitted-row preservation"
    - path: "tests/championship-organizer.spec.ts"
      provides: "Organizer persistence coverage"
  key_links:
    - from: "app/api/v1/admin/events/[id]/results/route.ts"
      to: "lib/server/admin/service.ts"
      via: "PUT rows normalization from legacy and canonical field identifiers"
      pattern: "sessionKind"
    - from: "lib/server/admin/service.ts"
      to: "lib/server/admin/repository.ts"
      via: "merge incoming rows with persisted rows before replace/write"
      pattern: "listEventResultsByEventId|replaceEventResults"
    - from: "app/api/v1/admin/championships/route.ts"
      to: "lib/server/admin/repository.ts"
      via: "organizerName passed through create and update paths"
      pattern: "organizerName"
---

<objective>
Make the admin write layer safe for the widened contract without pulling broad UI work into Phase 1.

Purpose: Persist canonical result rows and championship organizer metadata while preventing the current two-column editor from deleting unseen canonical fields.
Output: Updated admin repository/service/routes plus focused regression coverage.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-00-PLAN.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-01-PLAN.md
@lib/server/admin/types.ts
@lib/server/admin/repository.ts
@lib/server/admin/service.ts
@app/api/v1/admin/events/[id]/results/route.ts
@app/api/v1/admin/championships/route.ts

<interfaces>
From `lib/server/admin/service.ts`:
```ts
function validateResultCell(row: EventResultCellInput): EventResultCellInput
export async function updateEventResults(
  actor: AdminActor,
  eventId: string,
  input: { rows: EventResultCellInput[] },
  options?: RequestOptions,
): Promise<AdminWriteResult<{ rows: EventResultCellInput[] }>>
```

From `lib/server/admin/repository.ts`:
```ts
export async function getEventResultsGrid(eventId: string): Promise<AdminEventResultsGrid | null>
export async function replaceEventResults(
  eventId: string,
  rows: EventResultCellInput[],
): Promise<AdminEventResultRow[]>
```

From `app/api/v1/admin/events/[id]/results/route.ts`:
```ts
rows?: Array<{
  driverId: string;
  sessionKind: "primary" | "secondary";
  position: number | null;
  status: "DNF" | "DNQ" | "DSQ" | "ABSENT" | null;
  rawValue: string;
  isActive: boolean;
}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize canonical admin result writes and preserve omitted rows</name>
  <read_first>
    lib/server/admin/service.ts
    lib/server/admin/repository.ts
    app/api/v1/admin/events/[id]/results/route.ts
    lib/server/admin/types.ts
    .planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
  </read_first>
  <files>lib/server/admin/service.ts, lib/server/admin/repository.ts, app/api/v1/admin/events/[id]/results/route.ts, tests/admin-event-results-contract.spec.ts, tests/admin-event-results-preserve.spec.ts</files>
  <action>Update the admin write path so `updateEventResults()` accepts both canonical field identifiers (`qs`, `s`, `qf`, `f`, `p`) and legacy editor values (`primary`, `secondary`), normalizing legacy input to stored `s` and `f`. Keep `position = 0` valid for `p`. Change repository write behavior so omitted persisted rows survive Phase 1: merge the submitted rows with `listEventResultsByEventId(eventId)` by `(driverId, sessionKind)` before writing, instead of treating omission as deletion. Update `getEventResultsGrid()` to map stored `s` into the existing `primary` slot and stored `f` into the existing `secondary` slot so the current editor still loads, while silently preserving `qs`, `qf`, and `p` rows for Phase 2. Complete the Wave 0 scaffolds in `tests/admin-event-results-contract.spec.ts` and `tests/admin-event-results-preserve.spec.ts` with regression coverage for canonical input normalization, duplicate protection, and preserve-on-save behavior when incoming payloads omit canonical rows. DATA-02 in this phase is satisfied through the save path; keep canonical CLI import widening deferred.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/admin-event-results-contract.spec.ts tests/admin-event-results-preserve.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/admin-event-results-contract.spec.ts` exists and exercises canonical plus legacy `sessionKind` inputs.
    - `tests/admin-event-results-preserve.spec.ts` exists and covers omitted-row preservation.
    - `lib/server/admin/service.ts` contains normalization logic for `"primary"` -> `"s"` and `"secondary"` -> `"f"`.
    - `lib/server/admin/repository.ts` no longer deactivates every event row purely because it was omitted from the request snapshot.
    - `lib/server/admin/repository.ts` maps stored `s` and `f` rows into the current grid’s `primary` and `secondary` fields.
  </acceptance_criteria>
  <done>Phase 1 admin saves can add canonical data safely while the old two-column editor remains non-destructive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist organizer metadata through the admin championship APIs</name>
  <read_first>
    lib/server/admin/repository.ts
    lib/server/admin/service.ts
    app/api/v1/admin/championships/route.ts
    lib/server/admin/types.ts
    .planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
  </read_first>
  <files>lib/server/admin/repository.ts, lib/server/admin/service.ts, app/api/v1/admin/championships/route.ts, tests/championship-organizer.spec.ts</files>
  <action>Thread `organizerName` through `listAdminChampionships()`, `getChampionshipById()`, `createChampionshipRecord()`, `updateChampionshipRecord()`, `createChampionship()`, `updateChampionship()`, and the `/api/v1/admin/championships` `GET`, `POST`, and `PATCH` bodies. Validate organizer as optional trimmed text stored on `championships.organizer_name`; preserve `null` when empty. Complete the Wave 0 scaffold in `tests/championship-organizer.spec.ts` so it covers create/update/list behavior through the service and route-adapter seams.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/championship-organizer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/championship-organizer.spec.ts` exists.
    - `app/api/v1/admin/championships/route.ts` reads and passes `organizerName` in `POST` and `PATCH`.
    - `lib/server/admin/service.ts` validates and forwards `organizerName`.
    - `lib/server/admin/repository.ts` selects, inserts, and updates `organizer_name`.
    - `AdminChampionship` payloads returned by the admin route include `organizerName`.
  </acceptance_criteria>
  <done>Championship organizer metadata is fully persisted and retrievable across the admin write stack.</done>
</task>

</tasks>

<verification>
Run the new admin-focused tests and confirm no import-script or admin UI component changes were required for Phase 1 compatibility.
</verification>

<success_criteria>
Admin write paths support the widened contract safely, and the current two-column editor no longer risks dropping unseen canonical rows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-results-contract-ordering-and-championship-metadata/01-02-SUMMARY.md`
</output>
