---
phase: 01-results-contract-ordering-and-championship-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/009_canonical_results_contract.sql
  - lib/server/history/types.ts
  - lib/server/admin/types.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-04
must_haves:
  truths:
    - "Postgres can persist canonical result kinds `qs`, `s`, `qf`, `f`, and `p` without keeping `primary` or `secondary` as stored enum values."
    - "Championship records have a dedicated organizer field at the championship level."
    - "Shared TypeScript contracts expose one canonical field order that downstream admin and history layers can import."
  artifacts:
    - path: "db/migrations/009_canonical_results_contract.sql"
      provides: "Enum remap, organizer column, and points-safe numeric constraint"
    - path: "lib/server/history/types.ts"
      provides: "Canonical result field constants and public shared result types"
    - path: "lib/server/admin/types.ts"
      provides: "Admin-facing canonical input aliases and organizer field contracts"
  key_links:
    - from: "db/migrations/009_canonical_results_contract.sql"
      to: "event_results.session_kind"
      via: "legacy `primary` -> `s` and `secondary` -> `f` remap during type replacement"
      pattern: "update event_results.*case.*primary.*s.*secondary.*f"
    - from: "lib/server/admin/types.ts"
      to: "lib/server/history/types.ts"
      via: "shared canonical field unions imported from the history contract"
      pattern: "from \"@/lib/server/history/types\""
---

<objective>
Stabilize the Phase 1 storage contract before any service or route work changes behavior.

Purpose: Give downstream admin and history code one canonical field model and a safe schema target.
Output: One additive migration plus shared TypeScript result and championship contracts.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-VALIDATION.md
@.skills/version-aware-implementation/SKILL.md
@.skills/db-change-safety/SKILL.md
@db/migrations/001_results_schema.sql
@lib/server/history/types.ts
@lib/server/admin/types.ts

<interfaces>
From `lib/server/history/types.ts`:
```ts
export type SessionKind = "primary" | "secondary";
export type ResultStatus = "DNF" | "DNQ" | "DSQ" | "ABSENT";
```

From `lib/server/admin/types.ts`:
```ts
export type AdminChampionship = {
  id: string;
  seasonYear: number;
  name: string;
  slug: string;
  primarySessionLabel: string;
  secondarySessionLabel: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};

export type EventResultCellInput = {
  driverId: string;
  sessionKind: SessionKind;
  position: number | null;
  status: ResultStatus | null;
  rawValue: string;
  isActive: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical results migration</name>
  <read_first>
    db/migrations/001_results_schema.sql
    db/migrations/004_admin_auth_softdelete_audit.sql
    .planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
    .skills/db-change-safety/SKILL.md
  </read_first>
  <files>db/migrations/009_canonical_results_contract.sql</files>
  <action>Create `db/migrations/009_canonical_results_contract.sql` as a small Postgres migration that: (1) creates a replacement enum `session_kind_v2` with exactly `qs`, `s`, `qf`, `f`, and `p`; (2) alters `event_results.session_kind` through a `using case` remap from stored `primary` -> `s` and `secondary` -> `f`; (3) replaces the old `position > 0` check with a constraint that allows `position = 0` only when `session_kind = 'p'`, while keeping the existing mutual-exclusion rule between numeric value and status; and (4) adds nullable `organizer_name text` to `championships`. Keep the migration reversible in structure and focused on schema only. Do not widen `scripts/import-results-xlsx.ts` or `lib/server/history/parser.ts` in this phase because no canonical workbook fixture exists.</action>
  <verify>
    <automated>rg -n "create type session_kind_v2|values \\('qs', 's', 'qf', 'f', 'p'\\)|primary'.*'s'|secondary'.*'f'|organizer_name|session_kind = 'p'" db/migrations/009_canonical_results_contract.sql</automated>
  </verify>
  <acceptance_criteria>
    - `db/migrations/009_canonical_results_contract.sql` exists.
    - The migration text contains a `session_kind_v2` enum with `qs`, `s`, `qf`, `f`, and `p`.
    - The migration text remaps existing `primary` rows to `s` and `secondary` rows to `f`.
    - The migration text adds `championships.organizer_name`.
    - The migration text contains a points-aware numeric constraint for `event_results`.
  </acceptance_criteria>
  <done>The database contract can store canonical result kinds and championship organizer metadata without requiring synthetic historical rows.</done>
</task>

<task type="auto">
  <name>Task 2: Publish shared canonical result and organizer types</name>
  <read_first>
    lib/server/history/types.ts
    lib/server/admin/types.ts
    .planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
  </read_first>
  <files>lib/server/history/types.ts, lib/server/admin/types.ts</files>
  <action>Update `lib/server/history/types.ts` to export one canonical field source of truth: `CANONICAL_RESULT_FIELDS = ["qs", "s", "qf", "f", "p"] as const`, `CanonicalResultField`, and any public result-cell/result-entry shapes needed by later plans. Extend championship-facing public types with `organizerName: string | null`. Update `lib/server/admin/types.ts` to add `organizerName: string | null` to `AdminChampionship` plus explicit admin input/result aliases for canonical fields, including a compatibility input union that still allows `"primary"` and `"secondary"` for the unchanged Phase 1 editor path. Keep these files as contract definitions only; do not change admin UI components in this plan.</action>
  <verify>
    <automated>rg -n "CANONICAL_RESULT_FIELDS|type CanonicalResultField|organizerName|primary\" \\| \"secondary" lib/server/history/types.ts lib/server/admin/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/server/history/types.ts` exports `CANONICAL_RESULT_FIELDS`.
    - `lib/server/history/types.ts` exports a canonical field union type covering `qs`, `s`, `qf`, `f`, and `p`.
    - `lib/server/history/types.ts` includes `organizerName: string | null` on championship-facing read models.
    - `lib/server/admin/types.ts` includes `organizerName: string | null` on `AdminChampionship`.
    - `lib/server/admin/types.ts` defines an admin input/result kind union that preserves `"primary"` and `"secondary"` compatibility for Phase 1 writes.
  </acceptance_criteria>
  <done>Downstream admin and history implementations can import one canonical result contract instead of hardcoding field names or organizer metadata separately.</done>
</task>

</tasks>

<verification>
Confirm the migration file and shared types agree on the same canonical field identifiers: `qs`, `s`, `qf`, `f`, `p`. Confirm no import-script widening is attempted in this plan.
</verification>

<success_criteria>
Phase 1 has a concrete schema target and shared TypeScript contracts before repository/service work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-results-contract-ordering-and-championship-metadata/01-01-SUMMARY.md`
</output>
