---
phase: 01-results-contract-ordering-and-championship-metadata
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/server/history/repository.ts
  - lib/server/history/service.ts
  - app/components/event-participation-list.tsx
  - tests/history-repository.spec.ts
  - tests/history-api.spec.ts
  - tests/history-api-v2.spec.ts
  - tests/results-page.flow.spec.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Public results consumers receive canonical result fields in the order `QS`, `S`, `QF`, `F`, `P`."
    - "Participant ranking is driven by points first, with a final-race fallback when points are absent."
    - "Race-only aggregates ignore qualifying and points rows after the shared contract widens."
    - "Public championship read models can retrieve organizer metadata for later display work."
  artifacts:
    - path: "lib/server/history/repository.ts"
      provides: "Canonical result mapping, points-first participant ordering, and race-only aggregate filters"
    - path: "app/components/event-participation-list.tsx"
      provides: "Minimal canonical column ordering support for the existing public consumer"
    - path: "tests/history-repository.spec.ts"
      provides: "Points-first ordering and race-only aggregate coverage"
  key_links:
    - from: "lib/server/history/repository.ts"
      to: "app/components/event-participation-list.tsx"
      via: "participants array ordered by points-first helper and canonical session columns"
      pattern: "getSessionOrder|participants"
    - from: "lib/server/history/service.ts"
      to: "app/api/v1/results/events/route.ts"
      via: "event results and participation DTOs returned without extra UI-only transforms"
      pattern: "getResultsEvents|getResultsEventParticipation"
    - from: "lib/server/history/repository.ts"
      to: "current championship payload"
      via: "championship organizer field selected from `championships`"
      pattern: "organizer_name|organizerName"
---

<objective>
Expose the widened contract safely on the public read side, including participant ordering and race-only aggregate guards.

Purpose: Keep public consumers and stats-oriented queries correct as soon as canonical rows exist in Postgres.
Output: Updated history repository/service behavior, minimal public consumer compatibility, and regression coverage.
</objective>

<execution_context>
@/home/nico/.codex/get-shit-done/workflows/execute-plan.md
@/home/nico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-VALIDATION.md
@.planning/phases/01-results-contract-ordering-and-championship-metadata/01-01-PLAN.md
@lib/server/history/types.ts
@lib/server/history/repository.ts
@lib/server/history/service.ts
@app/components/event-participation-list.tsx
@app/results/page.tsx

<interfaces>
From `lib/server/history/repository.ts`:
```ts
function toParticipationCards(events: EventResultItem[]): EventParticipationCard[]
export async function getEventParticipationPage(query: EventQuery): Promise<{
  items: EventParticipationCard[];
  hasNext: boolean;
}>
export async function getCurrentChampionshipSummary(
  limitEvents = 5,
): Promise<CurrentChampionshipSummary | null>
```

From `app/components/event-participation-list.tsx`:
```ts
function getSessionOrder(
  sessionKind: EventParticipationEntry["sessions"][number]["sessionKind"],
): number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical public result mapping and points-first ordering</name>
  <read_first>
    lib/server/history/repository.ts
    lib/server/history/service.ts
    lib/server/history/types.ts
    tests/history-repository.spec.ts
    .planning/phases/01-results-contract-ordering-and-championship-metadata/01-RESEARCH.md
  </read_first>
  <files>lib/server/history/repository.ts, lib/server/history/service.ts, tests/history-repository.spec.ts</files>
  <action>Update the history repository to treat stored `session_kind` values as canonical fields. Replace the current best-position participant sort with a helper that orders by `p` descending when points exist, then falls back to final-race result `f` ascending for legacy rows without points, then by driver name. Preserve sparse historical rows without synthesizing missing fields. Update driver and overview aggregates so only final-race rows (`session_kind = 'f'`) contribute to wins, podiums, top-5, top-10, completed counts, and average position. Thread `organizerName` through championship-facing repository/service DTOs needed by the existing public routes.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/history-repository.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/history-repository.spec.ts` asserts points-first participant ordering.
    - `tests/history-repository.spec.ts` asserts the legacy fallback uses final-race ordering when `p` is absent.
    - `tests/history-repository.spec.ts` asserts race-only aggregates ignore `qs`, `s`, `qf`, and `p`.
    - `lib/server/history/repository.ts` contains explicit `session_kind = 'f'` filters for race-only aggregate queries.
    - `lib/server/history/repository.ts` selects `organizer_name` into at least one championship-facing public read model.
  </acceptance_criteria>
  <done>Public read models return canonical result data and stay correct when qualifying or points rows exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the public route and page consumer with canonical column order</name>
  <read_first>
    app/components/event-participation-list.tsx
    app/results/page.tsx
    tests/history-api.spec.ts
    tests/history-api-v2.spec.ts
    tests/results-page.flow.spec.ts
    lib/server/history/types.ts
  </read_first>
  <files>app/components/event-participation-list.tsx, tests/history-api.spec.ts, tests/history-api-v2.spec.ts, tests/results-page.flow.spec.ts</files>
  <action>Make the existing public consumer accept the widened participation model without redesigning the page. Update `getSessionOrder()` in `app/components/event-participation-list.tsx` so canonical field order is `qs`, `s`, `qf`, `f`, `p`, and keep column generation data-driven so historical events only render fields that actually exist. Add route and page-flow assertions that canonical fields are exposed to public consumers, participant ordering follows points, and current-championship payloads can include `organizerName`. Keep organizer display polish out of this phase; only the shared DTO and consumer compatibility belong here.</action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/history-api.spec.ts tests/history-api-v2.spec.ts tests/results-page.flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/event-participation-list.tsx` contains an explicit `qs`, `s`, `qf`, `f`, `p` ordering helper.
    - `tests/history-api.spec.ts` asserts canonical result-field exposure for `/api/v1/results/events`.
    - `tests/history-api-v2.spec.ts` asserts `organizerName` can appear in the current-championship payload.
    - `tests/results-page.flow.spec.ts` covers canonical column ordering or sparse historical column omission through the rendered public flow.
    - No task in this plan edits `app/results/page.tsx` or broader public styling beyond what is required for the existing consumer to read the canonical contract.
  </acceptance_criteria>
  <done>The public route/page path can consume canonical result fields immediately, while broader presentation polish remains in Phase 3.</done>
</task>

</tasks>

<verification>
Run the history-focused test group and confirm the public consumer shows only real columns while participant order matches the repository’s points-first rule.
</verification>

<success_criteria>
Canonical public results retrieval is safe for API and page consumers, and race-only aggregates remain trustworthy after the contract change.
</success_criteria>

<output>
After completion, create `.planning/phases/01-results-contract-ordering-and-championship-metadata/01-03-SUMMARY.md`
</output>
